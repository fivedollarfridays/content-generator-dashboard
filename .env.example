# Content Generator Dashboard - Environment Variables

# =============================================================================
# REQUIRED - Backend API Configuration
# =============================================================================

# Backend API base URL
# Development: http://localhost:8000
# Production: https://api.content-generator.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL for real-time updates
# Development: ws://localhost:8000
# Production: wss://api.content-generator.com
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# =============================================================================
# OPTIONAL - Analytics & Monitoring
# =============================================================================

# Google Analytics Measurement ID
# Get from: https://analytics.google.com
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking
# Get from: https://sentry.io
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# =============================================================================
# OPTIONAL - Feature Flags
# =============================================================================

# Enable analytics tracking
# NEXT_PUBLIC_ENABLE_ANALYTICS=true

# Enable debug logging (development only)
# NEXT_PUBLIC_ENABLE_DEBUG=true

# Enable batch operations UI
# NEXT_PUBLIC_ENABLE_BATCH_OPERATIONS=false

# =============================================================================
# OPTIONAL - API Configuration
# =============================================================================

# API request timeout (milliseconds)
# NEXT_PUBLIC_API_TIMEOUT=30000

# Maximum retry attempts for failed requests
# NEXT_PUBLIC_API_MAX_RETRIES=3

# =============================================================================
# IMPORTANT NOTES
# =============================================================================
#
# 1. Copy this file to .env.local for local development:
#    cp .env.example .env.local
#
# 2. Never commit .env.local to git (it's in .gitignore)
#
# 3. All variables prefixed with NEXT_PUBLIC_ are exposed to the browser
#    - Use for: API URLs, feature flags, public configuration
#    - Don't use for: Secrets, API keys, passwords
#
# 4. Variables without NEXT_PUBLIC_ are server-side only
#    - Only available during build time and in API routes
#    - Not accessible in browser/client components
#
# 5. Restart the development server after changing environment variables
#
# 6. For production deployment (Vercel):
#    - Add variables in Vercel Dashboard → Settings → Environment Variables
#    - Use production URLs for NEXT_PUBLIC_API_URL and NEXT_PUBLIC_WS_URL
#
# =============================================================================
